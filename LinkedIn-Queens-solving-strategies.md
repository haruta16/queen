# LinkedIn Queens 候选消元策略分级

本文档把 LinkedIn Queens 的解题策略重新表述为：

```text
观察棋盘
=> 标记不可能放 Queen 的空格 / X
=> 某行、某列、某颜色区域只剩唯一候选
=> 确认 Queen
=> 由新 Queen 继续标记更多 X
=> 循环
```

这比“寻找 Queen 的技巧”更贴近实际解题。因为牌局是静态的，所有 Queen 的真实位置在题目生成时已经固定。玩家做的事情不是创造答案，而是一步步证明哪些格子不可能是 Queen。  
大多数所谓高级技巧，本质都是“候选消元策略”，也就是证明某些格子应该被标记为 X。

## 0. 解题框架

### 0.0 基础规则

棋盘为 n×n 网格。每个格子属于恰好一个颜色区域（单连通块，不形成孤岛）。

```text
每行恰好 1 个 Queen
每列恰好 1 个 Queen
每个颜色区域恰好 1 个 Queen
任意两个 Queen 不能相邻（8 邻域，含斜向）
```

所有解题策略都由这四条规则推导而来。

### 0.1 核心观点

#### 0.1.1 策略核心是消元，不是放 Queen

因此，一个格子能不能放 Queen，只取决于它是否会违反这些规则。  
解题时真正复杂的部分，通常不是“直接看出某格是 Queen”，而是：

```text
这个格如果是 Queen，会不会冲突？
这个格是否被某个行 / 列 / 区域锁定排除？
这个格是否被某个候选集合的共同投影排除？
这个格是否会导致某个行 / 列 / 区域无处可放？
```

所以更准确的策略语言应该是：

```text
排除候选格
标记不可能格
缩小行 / 列 / 区域的候选集合
当候选集合缩小到 1 个时，再确认 Queen
```

Queen 的确认主要来自三个基础唯一规则：

```text
行唯一：某一行只剩 1 个候选格，则该格是 Queen
列唯一：某一列只剩 1 个候选格，则该格是 Queen
区域唯一：某颜色区域只剩 1 个候选格，则该格是 Queen
```

这三个规则并不复杂。复杂的是在它们触发之前，如何一步步把其他候选格标记为 X。

可以这样理解：

```text
高级策略负责制造 X
基础唯一规则负责确认 Queen
```

#### 0.1.2 直接确认 Queen 也可以转写成消元

有时你会感觉某个技巧“直接确定了 Queen”。但从逻辑上，它通常仍然可以改写为：

```text
排除同一行 / 同一列 / 同一区域里的其他所有候选
=> 该单位只剩一个候选
=> Queen 确认
```

例如：

```text
假设 r4c2 不是 Queen 会导致 A 区无处可放
=> r4c2 必须是 Queen
```

这是一种“必要候选”推理。它看起来像直接选择，但等价于：

```text
A 区其他候选都不可能
=> A 区只剩 r4c2
=> r4c2 是 Queen
```

所以，为了统一思维，本文把核心策略都写成“如何标记 X / 消除候选”。

### 0.2 基本术语

#### 0.2.1 候选格

候选格指当前还没有被证明不可能的格子。  
它可能最终是 Queen，也可能之后被标记为 X。

```text
候选格 = 当前仍可能放 Queen 的格子
```

#### 0.2.2 X 标记

X 标记表示：

```text
这个格子不可能是 Queen
```

X 不是装饰，也不是猜测。一个可靠的 X 应该能回答：

```text
为什么这个格子不可能放 Queen？
```

#### 0.2.3 单位

本文把以下三类都称为“单位”：

```text
一行
一列
一个颜色区域
```

每个单位都必须恰好拥有 1 个 Queen。

#### 0.2.4 资源

资源是某组候选会占用的东西，例如：

```text
某些行
某些列
某些颜色区域
某片空间
某些具体候选格
```

很多中高级技巧都可以表述为：

```text
若干单位刚好占住若干资源
=> 这些资源不能再给其他单位使用
=> 外部候选标记为 X
```

### 0.3 总循环

核心规则只有一条：

```text
任意策略一旦标出新 X，立刻回到 Level 1 从头开始。
```

因为新 X 可能让之前扫过的低级策略发现之前看不到的模式；而 X 若导致某单位只剩唯一候选，就确认 Queen，Queen 又触发新一轮 Level 1——这一切都自然包含在”回到 Level 1”里。

具体流程：

```text
1. 按策略顺序逐个执行。每个 Level 内部包含若干具体策略。
2. 当前策略一旦标出哪怕一个新 X → 立刻回到 Level 1 从头开始。
3. 当前策略未产出任何 X → 执行同 Level 的下一个策略。
4. 当前 Level 的所有策略都走完且无新 X → 进入下一级。
5. 若 Level 4 的全部策略也走完且无新 X → 盘面在当前信息下已无进展。
```

核心注意点：

```text
不要急着找 Queen。
先问哪些格子可以被证明不是 Queen。
Queen 往往是在 X 足够多之后自然出现的。
```

### 0.4 旧策略的重新归类

下表把常见说法统一改写为“候选消元 / 空格标记”语言。

| 原策略说法 | 新归类 | 新表达 |
| --- | --- | --- |
| 单点确定法 | Queen 确认器 | 某行 / 列 / 区域只剩 1 个候选后确认 Queen |
| 放置后的排除法 | Level 1 | 已知 Queen 产生直接 X |
| 行列唯一逻辑 | Queen 确认器 | 行 / 列候选被消元到 1 个后确认 Queen |
| 区域唯一逻辑 | Queen 确认器 | 区域候选被消元到 1 个后确认 Queen |
| 行锁定 / 列锁定 | Level 2 (k=1) | 单单位候选全部在另一单位中，外部候选标 X |
| 行列对子 / 区域对子 | Level 2 (k=2) | 两个单位占住两个资源，外部候选标 X |
| k 单位占 k 资源 | Level 2 (k≥3) | k 个单位占住 k 个资源，外部候选标 X（行/列/区域任意组合） |
| 候选组相邻排除 / 连续候选块 | Level 3 | 候选集合的直接相邻投影交集标 X |
| 容量溢出 | Level 3 | 高约束空间容量被占满后，其他单位在该空间内的候选标 X |
| 区域互锁 | Level 4 | 候选成立型反证的局部形态 |
| 反证法 / 强制链 | Level 4 | 假设候选成立或不成立，用强制步骤推出矛盾 |
| 双分支共同结论 | Level 4 | 多步分支推导后的共同结论；一阶相邻投影归 Level 3 |

## 1. Level 1：已知 Queen 的直接 X 标记

难度定位：基础。  
核心问题：

```text
已经确定的 Queen 会让哪些格子不可能？
```

这一层不需要复杂推理，只需要忠实传播规则。

Queen 确认后，以下四类格子**同时**标 X——它们是同一事件的直接产物，不存在逐项尝试：

- **同行**：Queen 所在行的其他所有格（每行恰 1 个 Queen）
- **同列**：Queen 所在列的其他所有格（每列恰 1 个 Queen）
- **同区域**：Queen 所在颜色区域的其他所有格（每区域恰 1 个 Queen）
- **相邻**：Queen 周围 8 个相邻格，含斜向（任意两个 Queen 不能相邻）

```text
同行：. . Q . .  =>  X X Q X X
相邻：  X X X
        X Q X
        X X X
```

标完 X 后立即检查：是否有行 / 列 / 区域只剩 1 个候选？若有，该候选确认为 Queen，新 Queen 继续触发上述四类 X。这个连锁循环会一直跑到不再确认出新 Queen 为止。

## 2. Level 2：集合占位 X 标记

难度定位：初中级 → 中级。  
核心问题：

```text
k 个单位的候选是否只分布在 k 个资源中？
如果是，这些资源被这 k 个单位独占——其他单位在这些资源上的候选全部标 X。
```

本质是 Hall 定理的实用版。从 k=1 到 k=n 全部是同一个逻辑，不需要为不同 k 值或不同单位/资源类型起新名字。

实战只需问三句话：

```text
几个单位？
它们总共只能去几个资源？
数量刚好相等的话，外部候选在哪里？
```

“单位”和”资源”都可以是行、列或颜色区域——任意组合都成立。

### 2.1 单单位锁定（k=1）

一个单位的所有候选都在一个资源中 → 资源被独占。

**行锁定：** 区域候选全部在同一行 → 该行中其他区域的候选标 X。反向：行候选全部在同一区域 → 该区域中其他行的候选标 X。

```text
A 区候选全在第 3 行 → 第 3 行非 A 区候选标 X
第 5 行候选全在 A 区 → A 区非第 5 行候选标 X
```

**列锁定：** 同理，把行换成列。

（横条、竖条、小块区域最容易触发 k=1，优先检查。）

### 2.2 双单位对子（k=2）

两个单位共享两个资源 → 两个资源被占满。

**行×列：** 两行候选只落在同两列 → 其他行在这两列上的候选标 X。反向亦然（两列只落在同两行）。

```text
第 1/4 行候选均为 C2 / C5 → 其他行的 C2、C5 候选标 X
```

**区域×行/列：** 两区域候选只分布在同两行 → 这两行中其他区域的候选标 X。列版本同理。

```text
A/B 区候选只在 R2 / R6 → R2、R6 中非 A 非 B 候选标 X
```

不需要知道具体谁在哪一行/列。

### 2.3 三单位占位（k=3）

三个单位共享三个资源。逻辑不变，出现频率低于 k=1/2。

```text
三行候选只落在同三列 → 这三列被这三行占满 → 其他行的这三列候选标 X
三区域候选只分布在同三行/列 → 这三行/列中其他区域的候选标 X
```

### 2.4 k ≥ 4 推广

逻辑不变：k 个单位 ⊆ k 个资源 → 外部候选标 X。k≥4 在标准棋盘上较少见，但检查方法一致。

### 2.5 检查清单

每轮问：

```text
是否有单位候选全部在同一行/列/区域？（k=1）
是否有 2 行/区域只占同 2 列/行？（k=2）
是否有 3 个单位只占 3 个资源？（k=3）
是否有 k 个单位只占 k 个资源？（k≥4）
这些资源中是否还有外部候选可以标 X？
```

## 3. Level 3：相邻投影与容量溢出

难度定位：中高级。  
核心问题：

```text
虽然还不知道某个 Queen 的具体位置，
但当前候选集合是否已经直接形成共同投影？
某片空间的 Queen 容量是否已被占满，导致其他单位在此空间内的候选可以直接排除？
```

这一层利用 Queens 独有的相邻禁放规则。  
它经常不是看单格，而是看一组候选的共同影响。  
Level 3 不进入多步假设分支；它只处理当前候选集合能直接算出的相邻投影和容量溢出。

这里的“直接排除”有严格边界：

```text
把某个候选临时当作 Queen
只执行一次 Level 1 规则：
同行、同列、同区域、相邻格标记 X
不继续触发唯一确认
不继续传播新 Queen
不继续使用 Level 2-3 的连锁消元
```

如果需要在分支里继续连锁推导，那已经进入 Level 4 假设型消元。

### 3.1 候选集合共同投影

回顾 Level 1：单个 Queen 会排除同行、同列、同区域、相邻格。  
在相邻行上，这表现为 `c - 1 / c / c + 1` 的直接禁区，其中 `c` 来自同列排除，`c - 1 / c + 1` 来自斜向相邻排除。

当 Queen 的具体位置尚未确定，但某个单位的候选集合是 `S` 时，可以计算：

```text
每个候选的一步 Level 1 直接投影
这些排除集合的交集是什么
```

交集里的格子，无论该单位最后选择哪个候选，都会被排除。  
因此这些格子可以直接标记 X。

例如，如果第 3 行还没有确定 Queen，但候选只在 C4 / C5，就可以分别计算 C4 与 C5 对相邻行造成的直接禁区，再把共同禁区标记为 X。

再看一个区域例子。某区域 A 只可能在两个格中：

```text
A 区候选：r3c3 / r3c4
```

则分别计算：

```text
r3c3 的直接相邻禁区
r3c4 的直接相邻禁区
两者的交集
```

交集中的候选格可以标记 X。

连续候选块不是另一种策略，只是共同投影最常见、最容易观察的形态。  
例如：

```text
某区域必须在第 4 行的 C3 / C4 中选一个
```

那么第 3 行和第 5 行靠近 C3 / C4 的某些格子，会因为两种选择都会冲突而被标记 X。

除求交集外，还有另一个方向：如果某个候选 c 作为 Queen，仅执行一次 Level 1 就直接让某个单位的候选数归零，则 c 可以直接标 X。这也只走了一步 Level 1，属于 Level 3。

```text
候选 c 若为 Queen
=> 同行 / 同列 / 同区域 / 相邻格标 X
=> 某行 / 列 / 区域候选数变为 0
=> c 标 X
```

这不需要链式推导，只需一轮排除即可判定。

### 3.2 容量溢出 X 标记

有些格子集合因为相邻禁放规则，能容纳的 Queen 数量有明确上限。

例如 2×2 方块中任意两个格子都相邻，最多容纳 1 个 Queen。

策略逻辑：

```text
1. 识别空间 S，确认容量上限 m
2. 找出所有”候选全部在 S 内”的单位 → 这些单位必定在 S 中放 Queen
3. 如果步骤 2 已经确认了 m 个单位必须占用 S，则 S 已饱和
4. 对于其他”候选部分在 S 内”的单位，S 内的候选全部标 X
```

不需要假设任何格是 Queen。只需要观察哪些单位的所有候选都被困在 S 内，且 S 是否已被占满。

典型例子：

```text
S = 2×2 方块，m = 1
区域 A 候选全部在 S 内 → A 必占 S（S 饱和）
区域 B：2 个候选在 S 内，3 个在 S 外
→ B 在 S 内的 2 个候选标 X
```

这个策略本质上是 Level 2（集合占位）在几何空间上的推广：资源不再是行/列，而是受相邻规则限制的任意格子集合。

注意：若发现超过 m 个单位的候选全部在 S 内，说明之前某步标记有误，需回溯检查——这不是策略的正常输出，而是错误信号。若在”候选 g 是 Queen”的假设下才出现容量矛盾，那属于 Level 4 反证法，容量矛盾只是在该分支中被发现的一种矛盾类型。

### 3.3 检查清单

每轮问：

```text
某个单位的候选集合是否共同压制同一批格子？
某个候选若为 Queen，是否在一轮 Level 1 后直接清空某个单位？
某 2×2 或其他高约束空间内，是否已有单位被完全困住？
饱和空间的容量是否已被占满，导致其他单位在此空间内的候选可以标 X？
```

## 4. Level 4：假设型消元

难度定位：高级。  
核心问题：

```text
如果临时假设某个候选成立或不成立，
能否用前四级规则推出矛盾或共同结论？
```

Level 4 的本质是”进入分支后继续消元”。  
区域互锁、强制链、双分支共同结论都属于假设型消元的不同表现，不再作为互相独立的策略。

注意：不要用”题目答案唯一，所以这里必须这样”来替代消元推理。如果两个分支看似都可行，说明附近还有未发现的约束——回头检查行、列、区域、相邻、锁定、集合占位、容量，找到实际能标 X 的理由，而不是靠唯一性跳过推理。

### 4.1 候选成立型反证

形式：

```text
假设某格 g 是 Queen
=> 执行 Level 1 直接排除
=> 继续触发 Level 2 / 3 消元
=> 某行 / 列 / 区域无候选
=> 假设错误
=> g 标记 X
```

区域互锁就是这个策略的局部形态：

```text
假设 A 区某候选 a 是 Queen
=> B 区所有候选都被排除
=> B 区无候选
=> a 不可能是 Queen
=> a 标记 X
```

构造反证链时，只追踪“不得不”的步骤：

```text
某行只剩 1 个候选
某列只剩 1 个候选
某区域只剩 1 个候选
某候选会清空另一个单位
某集合形成锁定
某集合形成占位
某候选集合形成共同投影
```

注意：如果只执行一次 Level 1 后就发现某单位候选数归零，这属于 Level 3 的单步致死，不需要进入 Level 4。Level 4 的特征是在分支内继续链式触发唯一确认、锁定、占位等更深层消元。

强制链不是独立策略，它只是执行候选成立型反证的方法。

### 4.2 候选排除型反证

有时反过来：

```text
假设某格 g 不是 Queen
=> 某单位只剩另一个候选
=> 连锁推导
=> 出现矛盾
=> g 必须是 Queen
```

这种看起来像“直接确定 Queen”。  
为了保持统一，可以这样理解：

```text
g 所在单位的其他安排全部不可能
=> 该单位只剩 g
=> g 确认 Queen
```

这类推理更难，建议只在候选数量很少时使用。

### 4.3 分支共同结论

如果某个单位有多个候选，可以分别进入分支。  
若所有分支都推出同一个结论，该结论成立。

共同结论可以是 X：

```text
若 A = 左，g 不可能是 Queen
若 A = 右，g 也不可能是 Queen
=> g 标记 X
```

共同结论也可以是 Queen：

```text
若 A = 左，B 区 Queen 必在 r6c2
若 A = 右，B 区 Queen 仍必在 r6c2
=> r6c2 是 Queen
```

为了保持“X 是过程，Queen 是结果”的框架，可以把共同 Queen 改写成：

```text
B 区除 r6c2 外的候选，在所有分支中都被排除
=> B 区只剩 r6c2
=> 确认 Queen
```

注意：如果共同结论只来自”一步相邻投影交集”，应归入 Level 3 的候选集合共同投影。  
Level 4 的分支共同结论，指需要在每个分支中继续执行若干步消元后才得到的共同结果。

### 4.4 检查清单

使用假设前先问：

```text
是否 Level 1-4 已经没有明显 X？
是否有候选数量很少的行 / 列 / 区域？
是否有一个候选看起来会强烈压迫邻区？
假设后能否在 2-4 步内看到矛盾或共同结论？
```

如果假设链太长，容易变成试猜。  
好的反证通常很短，而且每一步都由前四级规则支撑。

## 5. Queen 确认器

本章不是新的消元策略，而是唯一候选规则的集中定义，方便在循环中引用。  
每轮消元后，只检查三件事：

```text
某行是否只剩 1 个候选？
某列是否只剩 1 个候选？
某颜色区域是否只剩 1 个候选？
```

如果答案是“是”，该唯一候选就是 Queen。  
例如：

```text
X X ? X X
=> ? 是 Queen
```

确认出 Queen 后，按 0.3 总循环的规则，从 Level 1 重新开始——新 Queen 触发同行、同列、同区域、相邻格的直接 X 标记。

## 6. 实战工作流

### 6.1 推荐扫描顺序

核心规则：**任意一个策略**标出新 X，立刻回到 Level 1 从头开始。不是等整个 Level 执行完才判断。

按以下顺序逐个执行每个策略：

```text
Level 1（连锁循环：标记 X → 检查唯一 → 确认 Queen → 继续标记 X，直到不再产出新 Queen）
  ↓ 无新 X
Level 2 — k=1 行/列锁定 → k=2 行列/区域对子 → k=3 占位 → k≥4
  ↓ 无新 X
Level 3 — 共同投影（含单步致死） → 容量溢出
  ↓ 无新 X
Level 4 — 候选成立型反证 → 候选排除型反证 → 分支共同结论
  ↓ 无新 X
全部策略穷尽，当前盘面已无进展
```

任意一个 → 触发了新 X，就回到 Level 1 重头开始，而不是把同 Level 的其他策略也跑完。

这个顺序的思想是：

```text
越确定、越便宜的 X 越先做
任意新信息都从最便宜的 Level 1 开始消化
同 Level 内的策略也按成本从低到高排列
```


## 7. 四级难度总结

```text
Level 1：Queen 约束传播
核心：由已确认 Queen 排除同行、同列、同区域、相邻格，连锁确认新 Queen。

Level 2：集合占位 X 标记
核心：k 个单位的候选只分布在 k 个资源中 → 资源被独占 → 外部候选标 X。
      k=1 为锁定，k=2 为对子，k≥3 为占位。全部是同一逻辑。

Level 3：相邻投影与容量溢出
核心：候选集合形成直接共同投影，或高约束空间被占满后排除其他单位在此空间的候选。

Level 4：假设型消元
核心：临时假设某候选成立或不成立，用前三级规则推出矛盾或多步共同结论。
```

## 8. 最终抽象

LinkedIn Queens 的策略主线可以压缩成一句话：

```text
通过规则证明更多格子不可能是 Queen，直到某行、某列或某颜色区域只剩唯一候选。
```

更操作化地说：

```text
X 是过程，Queen 是结果。
消元是策略，唯一是确认。
高级技巧不是更神秘的放置方法，而是更强的空格标记方法。
```

只要始终围绕“这个 X 为什么成立”来思考，Queens 的解题过程就会从猜测变成稳定的逻辑推进。
