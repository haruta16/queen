# Queen 消元求解器：策略体系

本文档描述求解器（`src/game/solver.ts`）中实现的 6 种策略类型的设计原理、执行逻辑和批次合同。

---

## 1. 基础规则与核心概念

### 1.1 四条约束

```
每行恰好 1 个 Queen
每列恰好 1 个 Queen
每个颜色区域恰好 1 个 Queen
任意两个 Queen 不能相邻（8 邻域，含对角）
```

所有策略都从这四条规则推导。大多数策略的目标不是"找到 Queen"，而是**证明某些格子不可能是 Queen（标记 X）**。

### 1.2 候选格

候选格 = 当前棋盘上既不是 Queen、也不是 X、也不是 Wrong 的格子。它是可能放 Queen 的位置。

```typescript
// rules.ts — getCandidatesInRow / getCandidatesInCol / getCandidatesInRegion
// 过滤条件：!isQueen && !isX && !isWrong
```

### 1.3 单位

本文把以下三类统称为"单位"：

- 一行
- 一列
- 一个颜色区域

每个单位恰好容纳 1 个 Queen。

### 1.4 Queen 的约束传播（`applyQueen`）

确认一个 Queen 后，以下四类格子**同时**标记为 X：

- **同行**的所有其他格
- **同列**的所有其他格
- **同区域**的所有其他格
- **8 邻域**的所有格（含对角）

这是最基础的推理动作，由 `rules.ts` 的 `applyQueen` 函数实现。它一步完成"放置 Queen + 传播 X"，是 L1 策略的原子操作。

---

## 2. SolverBatch 批次合同

求解器的每一步产出被封装为一个 `SolverBatch`：

```typescript
type SolverBatch = {
  index: number;               // 从 1 开始的步序号
  strategy: StrategyType;      // 本步使用的策略类型
  eliminations: Position[];    // 本步新标记的 X
  queenConfirmed: Position[];  // 本步新确认的 Queen
  description: string;         // 人类可读的描述
};
```

**批次合同的四条规则**：

1. **无产出不记录**：策略如果没有产生任何消除或 Queen 确认，返回 `null`，不记录批次。
2. **一次调用一个批次**：每个策略函数每次调用最多返回一个批次。
3. **批次内不连锁**：批次内的 X 和 Queen 是同一推理动作的直接产物，不会在批次内触发新的策略推理。
4. **L1 每批次一个 Queen**：L1 每次只处理一个唯一候选格，确保每一步都是原子推理。

---

## 3. 求解器主循环

```typescript
// solver.ts — solve()
const STRATEGIES = [
  stepL1,             // 唯一候选 + Queen确认 + 传播
  stepL2Lock1,         // 单锁定
  stepL2Lock2,         // 双锁定
  stepL2Lock3,         // 三锁定
  stepL3Projection,    // 投影交集
  stepL3Contradiction, // 单步矛盾
];

while (棋盘未完成 且 迭代次数 < 500) {
  for (const fn of STRATEGIES) {
    batch = fn(currentBoard, batchIndex + 1);
    if (batch !== null) {
      记录 batch → applyBatch 应用 → break（回到最外层循环）
    }
  }
  if (全部策略无产出) break;  // 求解器在当前信息下无法继续
}
```

**关键规则**：任意策略一旦命中，立刻回到 L1 重新开始。因为新信息（X 或 Queen）可能让更简单的策略发现之前看不到的模式。

策略排列顺序从最简单/最快到最复杂/最慢。L1 确认 Queen 的优先级最高。

---

## 4. 六种策略详解

### 4.1 L1 — 唯一候选确认

**类型标识**：`'L1'`

**核心思想**：如果某行/列/区域只剩一个候选格，则该格一定是 Queen。确认后立即传播 X。

**执行逻辑**：

```
1. 调用 findUniqueCandidates(board)
   → 遍历所有行：每行的候选格列表长度 = 1 → 该格是唯一候选
   → 遍历所有列：同上
   → 遍历所有区域：同上
   → 返回所有唯一候选格（去重）

2. 取第一个唯一候选格 target

3. 调用 applyQueen(board, target)
   → 放置 Queen + 自动传播 X（同行/同列/同区域/邻域）

4. 返回 batch:
   - queenConfirmed: [target]
   - eliminations: newX（传播产生的所有新 X）
```

**设计要点**：

- 一次只处理一个 Queen。放置后棋盘状态改变，主循环回到 L1 重新检查——新的唯一候选可能已经出现。
- `applyQueen` 一步完成"放置 + 传播"，无需分离"先放 Queen"和"再传播 X"。
- 唯一的 `queenConfirmed` 产出者——其他 5 种策略只产出 X 消除。

**示例**：

```
第 2 行：候选格只剩 r2c4
→ L1 确认 Queen 于 r2c4
→ 传播消除 r2 行其余格、c4 列其余格、同区域其余格、邻域格
```

### 4.2 L2_Lock1 — 单锁定

**类型标识**：`'L2_Lock1'`

**核心思想**：一个单位的所有候选恰好落在另一个单位中 → 该另一个单位被"锁定"，其他单位在此处的候选可以消除。这是鸽巢原理 k=1 的特例。

**四种变体**（按检查顺序，命中即返回）：

| 变体 | 条件 | 消除目标 |
|---|---|---|
| 区域 → 行 | 区域 rid 的所有候选 row 相同 | 该行中**其他区域**的候选格 |
| 区域 → 列 | 区域 rid 的所有候选 col 相同 | 该列中**其他区域**的候选格 |
| 行 → 区域 | 某行的所有候选 regionId 相同 | 该区域中**该行以外**的候选格 |
| 列 → 区域 | 某列的所有候选 regionId 相同 | 该区域中**该列以外**的候选格 |

**判断方式**：`new Set(cands.map(c => c.row)).size === 1`（候选集在该维度上收敛到单一值）。

**产出**：仅 `eliminations`，`queenConfirmed` 为空。

**示例**：

```
区域 A 的候选全在第 3 行：
┌───┬───┬───┬───┐
│ A │ A │   │   │ ← r3：A 区候选全在此行
├───┼───┼───┼───┤
│   │   │   │   │
└───┴───┴───┴───┘
→ 第 3 行中非 A 区的候选格标 X
  因为第 3 行的 Queen "名额"已被 A 区占据
```

### 4.3 L2_Lock2 — 双锁定

**类型标识**：`'L2_Lock2'`

**核心思想**：两个单位的候选加起来，恰好只占两个其他单位 → 这两个单位被"锁定"，外部候选消去。鸽巢原理 k=2。

**四种变体**（按检查顺序）：

| 变体 | 条件（C(n,2) 枚举） | 消除目标 |
|---|---|---|
| 2 行 → 2 列 | 两行候选的 col 并集 size=2 | 其他行在这 2 列上的候选 |
| 2 列 → 2 行 | 两列候选的 row 并集 size=2 | 其他列在这 2 行上的候选 |
| 2 区域 → 2 行 | 两区域候选的 row 并集 size=2 | 这两行中**非这两个区域**的候选 |
| 2 区域 → 2 列 | 两区域候选的 col 并集 size=2 | 这两列中**非这两个区域**的候选 |

**判断方式**：对两单位取候选并集，检查其在目标维度上的去重数量是否恰好为 2。

**产出**：仅 `eliminations`，`queenConfirmed` 为空。

**示例**：

```
第 1 行候选 = {c2, c4}      第 1/3 行候选仅占 c2/c4 两列
第 3 行候选 = {c2, c4}      → 这两列的 Queen 名额被 r1/r3 锁定
第 0/2/4 行在 c2、c4 的候选标 X
```

### 4.4 L2_Lock3 — 三锁定

**类型标识**：`'L2_Lock3'`

**核心思想**：与双锁定完全相同，从 k=2 扩展到 k=3。鸽巢原理 k=3。

**两种变体**：

| 变体 | 条件（C(n,3) 枚举） | 消除目标 |
|---|---|---|
| 3 行 → 3 列 | 三行候选的 col 并集 size=3 | 其他行在这 3 列上的候选 |
| 3 列 → 3 行 | 三列候选的 row 并集 size=3 | 其他列在这 3 行上的候选 |

**组合数枚举**：使用 `combos3` 生成器，C(n,3) = n(n-1)(n-2)/6。n=7 时为 35 种组合，n=10 时为 120 种。

**产出**：仅 `eliminations`，`queenConfirmed` 为空。

**注意**：当前实现仅覆盖行↔列的 Lock3，未扩展到区域参与的三锁定。区域锁定的 k=3 模式在 n≤7 的典型棋盘上极为罕见，求解器可通过 Lock1/Lock2/L3 的组合连锁推导达到等效效果。

### 4.5 L3_Projection — 投影交集

**类型标识**：`'L3_Projection'`

**核心思想**：在一个候选单位（≥2 候选的行/列/区域）中，对每个候选格计算"若它为 Queen 会消除哪些格"的投影集合，取所有投影的**交集**——交集内的格子无论该单位最终选哪个候选都会被消除，必为 X。

**执行逻辑**：

```
1. collectCandidateUnits: 收集所有 ≥2 候选的行/列/区域
   返回 [{candidates, label}, ...]

2. 对每个单位：
   a. 对每个候选格，调用 candidateProjectionSet：
      → 同行所有格 / 同列所有格 / 同区域所有格 / 8 邻域格
      → 返回这些格的坐标集合（字符串 key）
   b. 取所有投影的交集：projections[0] ∩ projections[1] ∩ ...
   c. 交集中的格子 → 过滤掉已是 Queen/X/Wrong 的 → 标记为 X

3. 任一单位产生消除 → 返回 batch
```

**`candidateProjectionSet` 的本质**：模拟 `applyQueen(cand)` 的消除范围，但不实际修改棋盘。它穷举了一个候选格"如果是 Queen"会直接消除的全部格子。

**产出**：仅 `eliminations`，`queenConfirmed` 为空。

**示例**：

```
区域 A 候选 = {r3c2, r3c3}

若 r3c2 是 Queen → 消除 {..., r2c1, r2c2, r2c3, r4c1, r4c2, r4c3, ...}
若 r3c3 是 Queen → 消除 {..., r2c2, r2c3, r2c4, r4c2, r4c3, r4c4, ...}

交集 → r2c2, r2c3, r4c2, r4c3 等
→ 这些格无论区域 A 选哪个候选都会被消除，必为 X
```

**与 L2 锁定的区别**：锁定基于"候选分布在哪些行/列"的计数逻辑（鸽巢原理），投影基于"候选作为 Queen 会消除哪些具体格子"的几何逻辑（邻域禁放规则）。

### 4.6 L3_Contradiction — 单步矛盾

**类型标识**：`'L3_Contradiction'`

**核心思想**：假设某个候选格是 Queen → 计算其投影（即一次 `applyQueen` 的消除范围）→ 检查是否有任何行/列/区域因此候选数归零。如果有，说明假设导致矛盾，该候选格必为 X。

**执行逻辑**：

```
1. 收集所有候选单元（≥2 候选的行/列/区域）

2. 对每个单元，对单元中的每个候选格 cand：
   a. 计算 candidateProjectionSet(board, n, cand)
      → 若 cand 是 Queen 会消除的格子集合
   b. 检查每一行 r（r ≠ cand.row）：
      → 该行的所有候选格是否全部在投影集合中？
      → 是 → 矛盾！cand 标 X，返回 batch
   c. 检查每一列 c（c ≠ cand.col）：同上
   d. 检查每个区域 rid2（rid2 ≠ cand 的区域）：同上
```

**为什么是"单步"**：只模拟 `cand` 作为 Queen 后的一轮直接消除（Level 1 传播），不继续触发 L2 锁定、L3 投影等深层推导。因此只能检测"一步致死"的矛盾场景。

**产出**：仅 `eliminations`（每次一个候选格），`queenConfirmed` 为空。

**示例**：

```
候选格 r4c2，假设它是 Queen：
→ 投影消除 r4 行其余格、c2 列其余格、同区域其余格、邻域格
→ 检查第 3 行：原本候选 = {r3c0, r3c1}
  → r3c0 和 r3c1 都在投影中（邻域 + 同列）
  → 第 3 行候选数归零
→ 矛盾！r4c2 不可能是 Queen → 标 X
```

---

## 5. 策略能力矩阵

| 策略 | 推理方式 | 产出 Queen | 产出 X | 复杂度 | 出现频率 |
|---|---|---|---|---|---|
| L1 | 唯一候选计数 | ✅ | ✅ | O(n²) | 极高 |
| L2_Lock1 | k=1 鸽巢（4变体） | — | ✅ | O(n²) | 高 |
| L2_Lock2 | k=2 鸽巢（4变体） | — | ✅ | O(n³) | 中 |
| L2_Lock3 | k=3 鸽巢（2变体） | — | ✅ | O(n⁴) | 低 |
| L3_Projection | 投影交集（几何） | — | ✅ | O(n³) | 中低 |
| L3_Contradiction | 单步归谬 | — | ✅ | O(n⁴) | 低 |

**策略排序原则**：越确定、越便宜的越先执行。L1 确认 Queen 永远最优先（Queen 产生大量 X，信息增益最高）。L2 锁定其次（纯计数，速度快）。L3 投影和矛盾兜底（几何推理，需要更多计算）。

---

## 6. 策略命名说明

本项目的策略命名是**扁平的**（6 种 StrategyType），不分组为 Level 1/2/3/4 的层级结构。

命名中的 "L1"、"L2"、"L3" 前缀表示策略的**复杂度级别**，不是层级分组：

- **L1**：仅依赖候选计数的直接推理（找唯一候选）
- **L2**：鸽巢原理占位推理（k 单位锁定 k 资源）
- **L3**：几何投影与假设验证推理（需要计算投影集合或模拟假设分支）

求解器按复杂度从低到高依次尝试：L1 → L2_Lock1 → L2_Lock2 → L2_Lock3 → L3_Projection → L3_Contradiction。任一命中即回到 L1 重头开始。

---

## 7. 求解器能力边界

当前求解器可以处理 n=5~10 的绝大多数合法 Queen 棋盘。已知的能力边界：

1. **L3_Contradiction 仅做单步假设验证**。如果矛盾需要 2 步以上的连锁推导才能暴露（例如：假设 A 是 Queen → B 被消除 → 触发 L2 锁定 → 另一单位候选归零），当前求解器无法检测。

2. **不实现多步假设型消元**。设计上不包含"进入假设分支后用全部策略做深层推导直到发现矛盾"的能力。这一步留待未来扩展。

3. **不实现容量溢出策略**。对于"2×2 方块中某区域的所有候选都在其中 → 方块饱和 → 其他区域在该方块的候选消除"这类几何容量推理，当前求解器通过 L3_Projection + L3_Contradiction 的组合间接处理。

4. **求解器是确定性的**（无随机性）。同一棋盘输入总是产生相同的批次序列。不依赖"猜然后验证"的策略。

---

## 8. 核心总结

```
Queen 消元求解器 = 6 种策略的优先级循环

L1：      某行/列/区域只剩 1 个候选 → 确认 Queen + 传播 X
L2_Lock1：某单位候选全落在一个资源 → 锁定资源，外部候选标 X
L2_Lock2：2 单位候选仅占 2 资源 → 锁定资源，外部候选标 X
L2_Lock3：3 单位候选仅占 3 资源 → 锁定资源，外部候选标 X
L3_Projection：单位候选的投影取交集 → 交集格必为 X
L3_Contradiction：假设候选是 Queen → 某单位候选归零 → 候选标 X

循环：任一命中 → 记录批次 → 回到 L1 重头开始
终止：全部策略无产出，或棋盘已完成
```
