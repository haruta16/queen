# 验收标准

## 1. MVP 验收

只有以下条目全部成立时，MVP 才算可接受。

## 2. 关卡生成器

- 生成器可根据 n（5-10）生成合法 Queen 关卡。
- 生成器可通过 targetSteps 参数控制关卡的策略步骤总数（在误差容忍范围内）。
- 生成器生成的每个关卡有且仅有唯一解。
- 生成器生成的每个关卡可用 6 种策略完全求解，不依赖多步假设型消元。
- 对于给定 n，生成器可以产出不同策略步骤总数的关卡（可区分简单/中等/困难）。
- 生成的关卡中，策略步骤总数少的关卡确实推理链更短，策略步骤总数多的关卡确实推理链更长。
- 相同随机种子产生相同关卡（确定性可复现）。
- 5×5 到 10×10 棋盘生成时间在可接受范围（< 15 秒/关）。
- 关卡数据包含完整的最优解策略类型序列（`strategySequence`）。

## 3. 求解器

- 求解器实现全部 6 种策略类型：
  - L1：检查行/列/区域只剩唯一候选 → 确认 Queen → 传播 X（同行/同列/同区域/邻域）。
  - L2_Lock1：单单位锁定（k=1 鸽巢，区域↔行/列）。
  - L2_Lock2：双单位锁定（k=2 鸽巢，行/列/区域组合）。
  - L2_Lock3：三单位锁定（k=3 鸽巢，行↔列）。
  - L3_Projection：候选集合投影取交集，交集格必为 X。
  - L3_Contradiction：假设候选是 Queen → 单步投影 → 某单位候选归零 → 矛盾 → 候选标 X。
- 求解器策略执行顺序正确：任意策略命中 → 立即回到 L1 重新开始。
- 求解器对每个消除批次正确标注策略类型（`StrategyType`）。
- 求解器输出完整的 `SolverBatch[]` 序列。
- 求解器对合法唯解棋盘能给出完整步骤序列。
- 求解器对所有 6 种策略均无产出时返回 `complete: false`。
- 求解器的批次合并粒度一致（相同棋盘状态产生相同批次序列）。

## 4. 玩家操作

- 玩家可以点击空格标记 X。
- 玩家可以点击候选格确认 Queen（需满足唯一候选条件）。
- 玩家尝试在非唯一候选格确认 Queen 时，有清晰错误反馈。
- 玩家尝试放置违反规则的 Queen 时，有清晰错误反馈。
- 撤销按钮可以回退最近一次 X 标记。
- 重做按钮可以恢复被撤销的 X 标记。
- 棋盘重置按钮恢复关卡初始状态。
- Queen 确认后自动传播同行/同列/同区域/相邻的 X。

## 5. UI 清晰度

- 棋盘正确显示 n×n 网格。
- 颜色区域使用可区分的色块。
- Queen 使用明确的图标或符号。
- X 标记使用半透明遮罩或斜线，清晰区别于空格和 Queen。
- 当前棋盘大小和策略步骤总数可见。
- 撤销和重做按钮容易找到。
- 求解器面板入口容易找到。
- 生成器面板入口容易找到。
- 求解器面板显示完整策略批次列表：序号、策略类型、消除数量、Queen 确认、可读描述。
- 生成器面板有 n 选择和复杂度选择（简单/中等/困难），内部映射为 targetSteps。
- 桌面端布局不溢出。
- 移动端（320px 宽度起）布局可用。
- 5×5 到 10×10 棋盘均可读。
- 求解器面板桌面端侧边滑入，移动端底部滑入。

## 6. 动效

- 标记 X 有反馈动效（缩放 + 淡入，~200ms）。
- 确认 Queen 有反馈动效（放大闪烁 + 弹入，~300ms）。
- Queen 确认后 X 传播有连锁动效（波纹扩散，~400ms）。
- 错误操作有反馈动效（红色闪烁 + 回弹，~300ms）。
- 关卡完成有反馈动效（全屏闪光 + 提示，~500ms）。
- 动效不阻塞后续操作。
- 传播动效可跳过（点击或再次操作时立即完成）。

## 7. 测试

至少需要覆盖：

- 规则函数：邻接检查、候选计算、唯一候选查找。
- 规则函数：Queen 放置后的 X 标记范围。
- 求解器：仅需 L1 策略可求解的棋盘。
- 求解器：需要 L2 策略可求解的棋盘。
- 求解器：需要 L3 策略可求解的棋盘。
- 求解器：不可解棋盘返回 incomplete。
- 求解器：批次序列中策略类型标注正确。
- 生成器：Queen 位置生成不违反约束。
- 生成器：区域连通性正确。
- 生成器：不同 targetSteps 产出不同复杂度关卡。
- 生成器：生成→求解→验证完整链路。
- 生成器：种子确定性。

## 8. 浏览器验证

报告完成前，需要验证：

- `npm run dev` 可以启动。
- `npm test` 全部通过。
- `npm run build` 无错误。
- 应用渲染出非空棋盘。
- 生成 5×5 简单关卡并完成游玩。
- 生成 7×7 中等关卡并完成游玩。
- 生成 10×10 困难关卡并用求解器辅助验证。
- 求解器面板可打开并正确显示策略批次序列。
- 撤销和重做功能正常。
- 移动端视口可用。
- 10×10 棋盘在移动端可操作。

## 9. 拒收标准

出现以下情况时，工作不可接受：

- 生成器无法稳定产出合法关卡。
- 生成器产出的关卡有多解或无解。
- targetSteps 参数对生成结果无影响（所有参数产出相似难度的关卡）。
- 求解器无法对合法关卡给出完整求解步骤。
- 求解策略类型标注错误。
- 核心规则或求解逻辑嵌入在 React 组件内部。
- 没有自动化测试。
- 玩家无法在浏览器中标记 X 和确认 Queen。
- 撤销/重做功能缺失或不正确。
- 求解器面板不显示步骤或不支持播放控制。
- 生成器面板参数调整后不生效。
- 视觉表现只是纯表格，没有动效反馈。
- 移动端或桌面端完全无法使用。
- 实现中加入了 MVP 范围外的复杂系统（Level 4、排行榜、用户系统等）。
