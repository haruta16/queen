# AI 开发框架

## 1. 目的

本文档定义后续 AI Agent 如何独立、安全地开发这个 Queen 解谜游戏原型。

本仓库是一个规划工作区，包含：

- 产品与玩法设计。
- 策略体系详细分析。
- AI 执行规则。
- 未来实现阶段。
- 验收标准。
- 任务 backlog。

AI Agent 应按照本文档定义的框架有序推进开发。

## 2. 推荐开发模式

MVP 推荐使用单 Agent 长任务完成。

原因：

- 求解器、生成器、游戏规则高度耦合。
- 求解器的批次合并粒度必须与生成器的计数预期一致——两个模块由同一 Agent 在连续上下文中实现可避免不一致。
- 棋盘状态、策略执行、UI 交互必须共享同一套状态模型。
- 单一连续上下文可以减少互相矛盾的实现假设。

只有在 MVP 已经可玩之后，再使用并行或后台 Agent 处理：

- 更多参数组合的批量生成和验证。
- 动画 polish。
- 视觉主题探索。
- 性能优化。
- 求解器策略覆盖率分析。

## 3. 工作流拆分

开发工作拆为以下线：

| 工作线 | 目的 | 负责人模式 |
|---|---|---|
| 类型定义 | 所有数据结构（含 SolverBatch、SolverResult 等），提供类型基础 | 主 Agent |
| 规则引擎 | 纯规则函数（邻接检查、候选计算、唯一判断） | 主 Agent |
| 求解器 | 全部 7 种策略类型实现，Level 1→2→3 主循环，批次输出 | 主 Agent |
| 生成器 | Queen 布局生成 + 区域生长 + solver 验证 + targetSteps 控制 | 主 Agent |
| 状态管理 | 持有游戏状态，调用核心函数，暴露 UI 操作 | 主 Agent |
| 可玩 UI | React 棋盘、HUD、工具栏、求解器面板、生成器面板 | 主 Agent |
| 动效与适配 | CSS 动画、移动端适配、视觉 polish | 主 Agent 或辅助 Agent |
| 验证 | 单元测试、浏览器检查、验收清单 | 主 Agent |

MVP 期间核心模块（rules/solver/generator）不要分给多个 Agent 同时修改。每个核心模块应有一个明确负责人。

## 4. 开发顺序

关键依赖链：

```text
types.ts → rules.ts → solver.ts → generator.ts
                                    ↘
                                  store.ts → UI 组件
```

**求解器必须先于生成器完成**：生成器依赖求解器返回的 totalSteps 来判断生成的关卡是否达到目标策略步骤总数。

推荐 Phase 顺序：

1. Phase 0 — 准备：确认开发环境，初始化项目。
2. Phase 1 — 应用脚手架：Vite + React + TypeScript。
3. Phase 2 — 类型定义：所有数据结构（重点：SolverBatch、SolverResult、Level）。
4. Phase 3 — 规则引擎：纯规则函数 + 测试。
5. Phase 4 — 求解器：全部 7 种策略类型 + 主循环 + 测试。
6. Phase 5 — 生成器：Queen 布局 + 区域生长（含复杂度参数）+ solver 验证 + targetSteps 控制 + 测试。
7. Phase 6 — 状态管理：连接核心逻辑和 UI。
8. Phase 7 — 可玩 UI：棋盘、HUD、工具栏、求解器面板、生成器面板。
9. Phase 8 — 动效与适配：CSS 动画、移动端适配、视觉 polish。
10. Phase 9 — 验证：全量测试、build、浏览器验证。

每一步结束时都应尽量保持项目可运行。

## 5. 长任务护栏

AI 自主实现时，不要在核心逻辑正确前追求视觉 polish。

优先级顺序：

1. 类型定义正确且完整（SolverBatch 等核心类型不缺字段）。
2. 规则逻辑正确。
3. 求解器可正确求解，产出完整 SolverBatch[] 序列，策略类型标注正确。
4. 生成器可生成合法关卡。
5. 生成器可通过 targetSteps 产出不同复杂度的关卡。
6. 棋盘可交互（标记 X / 确认 Queen）。
7. 求解器可可视化展示（逐步骤播放）。
8. 生成器面板可用（n + 复杂度选择）。
9. 基础动效。
10. 移动端适配。
11. 视觉 polish。

如果时间有限，优先交付逻辑正确、可玩的原型，而不是漂亮但不稳定的页面。

## 6. 决策记录格式

实现开始后，如果做出重要设计选择，在 `DECISIONS.md` 中添加简短记录。

建议格式：

```md
## YYYY-MM-DD - 决策标题

决策：
理由：
考虑过的替代方案：
影响：
```

值得记录的决策示例：

- 求解器批次合并粒度：何时合并相邻的同类型消除为一个批次，何时拆分为多个批次。
- 区域生长算法的复杂度参数如何定义和调节。
- 生成器中 targetSteps 的调整反馈循环的具体实现（增加/减少复杂度的操作）。
- targetSteps 的 tolerance 如何设定（绝对值 ±N 还是百分比）。
- 撤销是否支持撤销 Queen 确认（MVP 阶段建议不支持）。
- 生成器性能优化策略（即时生成 vs 预生成池）。

## 7. 交接给后续 AI 的提示词

开始实现时可以使用：

```text
读取 AGENTS.md、Queen解谜游戏原型设计.md、AI_DEVELOPMENT_FRAMEWORK.md、AI_IMPLEMENTATION_PLAN.md 和 ACCEPTANCE_CRITERIA.md。

开始 Vite + React + TypeScript 原型的 Phase 1。创建应用脚手架，建立目录结构，添加 Vitest。注意区分策略等级（分类）和策略步骤总数（数量）。完成后停止并报告。
```

要求完整 MVP 开发时可以使用：

```text
读取全部项目文档。独立实现 AI_IMPLEMENTATION_PLAN.md 和 ACCEPTANCE_CRITERIA.md 中描述的完整 MVP。使用 Vite + React + TypeScript。求解器实现全部 7 种策略类型并输出完整 SolverBatch[] 序列。生成器支持通过 targetSteps 控制关卡复杂度。实现可交互棋盘 UI 和求解器可视化面板。加入几何动感风格的视觉表现和交互动效。运行测试和 build，启动 dev server，并报告本地 URL 与已知限制。
```

## 8. 避免事项

避免：

- 做成普通 N-Queen 算法可视化。
- 混淆策略等级（分类）和策略步骤总数（数量）。
- 在求解器未完成前先实现生成器。
- 在核心逻辑正确前先写复杂 UI。
- 把求解逻辑嵌入 React 组件。
- 设计需要 Level 4 猜测才能解的关卡（生成器应过滤掉）。
- 求解器批次粒度不一致（会导致生成器的 targetSteps 失效）。
- 第一版就加入大量动效阻碍操作流程。
- 忽略移动端适配。
- 在规则行为可测试前先写复杂 CSS 动画。
- Web 原型验证好玩之前引入 Canvas/WebGL 或其他渲染方案。
